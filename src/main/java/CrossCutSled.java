/**
 * This class represents a sled that attaches to a table saw, allowing the user to make
 * cross-cuts on the table saw safely.
 */

public class CrossCutSled extends Jig{
  CrossCutSled(){
    this.value = Const.VALUE_JIG_CROSSCUT_SLED;
  }
}
